<Window x:Class="OcrAppWPF.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OcrAppWPF.Views"
        mc:Ignorable="d" FontSize="18"
        Title="OcrApp" WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        
        <!-- Row 1 -->
        <Button x:Name="LoadDataPage" Grid.Row="1" Grid.Column="1" > Page 1 </Button>
        <Button x:Name="LoadFilesPage" Grid.Row="1" Grid.Column="2"> Page 2 </Button>

        <!-- Row 2 -->
        <ContentControl Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" x:Name="ActiveItem"/>
    </Grid>
</Window>
